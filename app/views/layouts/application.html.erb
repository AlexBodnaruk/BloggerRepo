<!DOCTYPE html>
<html>
  <head>
    <title>Blogger.gg</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"%>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <header>
  <nav>
    
    
    <ul>
    <li><a id = "mya" href="/">Blogger</a></li>
    <li >
      <% if user_signed_in? %>
        <%= form_with(url: destroy_user_session_path, method: :delete, html: { class: "d-inline" }) do |f| %>
          <%= f.button "Log out (#{current_user.NickName})", class: "nav-link btn-link", style: "background: none; border: none; cursor: pointer; padding: 0; color: #b3b6ba" %>
        <% end %>
      <% else %>
        <%= link_to "Log in", new_user_session_path, class: "nav-link" , style:"color: #b3b6ba " %>
      <% end %>
    </li>

      <li><a id = "mya" href="/posts/new">Add post</a></li>
      <li><a id = "mya" href="/categories/new">Add category</a></li>
    


    </ul>
  </nav>
</header>
    <%= yield %>
  </body>
  <style>
  body {
    margin: 0;
    padding: 0;
  }
  header {
  background-color: #2c2d30;
  padding: 10px;
}
  nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  font-weight: bold;
  text-decoration: none;
}

ul {
  list-style-type: none;
  margin: 0;
  padding: 0;
}

li {
  display: inline-block;
  margin-left: 10px;
}

a#mya  {
  text-decoration: none;
  color: #b3b6ba;

}

a:hover {
  text-decoration: underline;
}

</style>
</html>
