<div class="container">
  <h1 class="mt-4">Editing category</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", category: @category %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "Show this category", @category, class: "btn btn-primary" %> |
    <%= link_to "Back to categories", categories_path, class: "btn btn-secondary" %>
  </div>
</div>
