<%= form_with(model: category, class: "card") do |form| %>
  <% if category.errors.any? %>
    <div class="card-body bg-danger text-white">
      <h5 class="card-title"><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h5>

      <ul class="list-unstyled">
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card-body">
    <div class="mb-3">
      <%= form.label :categoryName, class: "form-label" %>
      <%= form.text_field :categoryName, class: "form-control" %>
    </div>

    <div class="text-center">
      <%= form.submit "Save", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>

